/// <reference types="node" />

/**
 *
 * @export
 * @param {string} sourcePath 拷贝源的绝对路径
 * @param {string} destinationPath 目标位置的绝对路径
 * @param {FileHandle} [handle] 对文件和文件夹的单独处理
 */
export declare function copyDir(sourcePath: string, destinationPath: string, handle?: FileHandle): void;

/**
 * 创建项目
 * @param projectDir 项目路径
 */
export declare function create(destinationPath: string, options: CreateOptions): void;

export declare type CreateOptions = {
    engineName: string;
};

/**
 *
 * @export
 * @interface FileHandle
 * @method directoryHandle 对文件夹的过滤，返回true 不做处理
 * @method fileHandle 对文件进行处理，返回处理后的数据
 */
export declare interface FileHandle {
    /**
     * 文件夹过滤
     * @param {string} directoryPath 文件夹的绝对路径
     */
    directoryHandle?: (directoryPath: string) => boolean;
    /**
     * 文件夹过滤
     * @param {string} filePath 文件的绝对路径
     * @param {string} fileData 文件的数据
     * @param {string} destinationPath 目标文件夹路径
     */
    fileHandle?: (filePath: string, fileData: Buffer, destinationPath: string) => Buffer;
}

export declare function getEgretProRoot(): string;

export declare function setEgretProRoot(value: string): void;

export { }
