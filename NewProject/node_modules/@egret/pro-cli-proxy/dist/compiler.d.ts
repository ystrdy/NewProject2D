
export declare type BuildOptions = {};

export declare class EgretCompiler {
    private cwd;
    static getOptionsConfig(): {
        method: string;
        description: string;
        options: {
            name: string;
            description: string;
            type: string;
        }[];
    }[];
    constructor(cwd: string);
    /**
     * 启动
     */
    start(options: StartOptions): Promise<void>;
    /**
     * 构建
     */
    build(options: BuildOptions): Promise<void>;
    /**
     * 发布
     */
    publish(options: PublishOptions): Promise<void>;
}

export declare type OptionsConfig = {
    method: string;
    description: string;
    options: {
        name: string;
        description: string;
        type: string;
    }[];
}[];

export declare type PublishOptions = {};

export declare type StartOptions = {
    /**
     * 是否需要 reporter，编辑器需要此字段，默认设置为 false
     */
    reporter: string;
    /**
     * 发布平台
     */
    target: "serverdebug" | "wxgame" | "web";
};

export { }
